/**
 *              Home Page Body
 *
 * 1. Open the home_page.dart file
 * 2. In the build method, replace the Container() widget with a Scaffold() widget
 * 3. Add an appBar: argument to the Scaffold widget -> abbBar: AppBar()
 *    return Scaffold(
        appBar: AppBar(),
      );
 * 4. Go to the terminal where the app runs, hit 'r' to hot reload, you should now see
   a white screen with a blue toolbar.
 * 5. Add a title to the tool bar, expand the AppBar constructor, add a title: argument
      with a Text() widget., do hot reload again ('r')
   6. You should now see the title on the toolbar.
   7. In the Scaffold right after the appBar field, add a body: argument
   8. Set the body: argument to Center() widget
   9. Set the Center() child: widget with a Text().

       Center(
         child: Text("Hello Flutter")
       )

   10. Do hot reload or restart the app.
 * * */