
/**
 * 1. Create application.dart file
 * 2. Import the 'package:flutter/material.dart' package
 * 3. Create MyApp class and extends StatelessWidget
 * 4. Override the build() method, and return a MaterialApp Widget
 * 5. Create a single main.dart file in lib folder
 * 6. Add void main() function and call runApp() method to run the MyApp just created
 * 7. In the MaterialApp constructor add title: "Flutter Workshop"
 * 8. In the MaterialApp constructor add Container()
 * 9. Open a terminal, type: flutter run -d all to run the app (We should get black screen now)
 * */