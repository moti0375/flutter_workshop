
/**
 *         Create Application
 *
 *
 * 1. Under the lib root folder, create application.dart file
 * 2. Import 'package:flutter/material.dart' package
 * 3. Create MyApp class and extends StatelessWidget
 * 4. Override the build() method, and return a MaterialApp Widget
 * 5. In the MaterialApp constructor add title "Flutter Workshop" in the title: argument
 * 6. In the MaterialApp constructor set a Container() to the home: argument
 * 7. Create a single main.dart file under the lib folder
 * 8. Import 'package:flutter/material.dart' package
 * 9. Add void main() function and call runApp() method to run the MyApp just created
 * 10. Open a terminal, type: flutter run -d all to run the app (You should get black screen now)
 * */